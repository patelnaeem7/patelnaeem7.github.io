# CV asset

The portfolio currently links to `naeem-patel-cv.pdf` through the `cv` value in
`assets/js/main.js`.

Confirm that this file is the final, proofread CV before deployment. If the file
is deliberately removed, clear the `cv` configuration value so the website
intercepts the download actions instead of sending visitors to a missing file.
